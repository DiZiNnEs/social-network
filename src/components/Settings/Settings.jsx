import React from 'react';
import classes from './styles/Settings.module.css'

const Settings = (props) => {
  return (
    <div>
      <p className={classes.p__settings}>settings</p>
    </div>
  );
}

export default Settings;
